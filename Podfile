# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'UnpackedViewController' do
    use_frameworks!
    
    pod 'Cartography', '~> 0.6'
    pod 'RealmSwift', '~> 1.0'
    pod 'PromiseKit', '~> 3.5'
    pod 'Money', '~> 1.7'
    
    target 'UnpackedViewControllerTests' do
        inherit! :search_paths
        # Pods for testing
    end
    
    target 'UnpackedViewControllerUITests' do
        inherit! :search_paths
        # Pods for testing
    end
end
